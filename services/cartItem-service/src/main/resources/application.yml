server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cartitemdb?useSSL=false&serverTimezone=UTC
    username: root
    password: sapassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
