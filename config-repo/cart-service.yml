server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cart_service?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
