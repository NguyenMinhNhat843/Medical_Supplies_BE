server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://mysql-db:3306/cartItem_service?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka